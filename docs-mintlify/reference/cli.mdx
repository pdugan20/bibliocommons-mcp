---
title: CLI reference
description: The bibliocommons-mcp command-line interface.
icon: terminal
---

{/* GENERATED by scripts/gen_cli_reference.py; do not edit by hand. */}

The `bibliocommons-mcp` console script. With no arguments it runs the MCP server
over stdio (for desktop clients); other subcommands are below. See
[Install](/install) for client wiring and [Configuration](/reference/configuration)
for settings.

```text
bibliocommons-mcp [subcommand]

Console-script entry point.

Subcommands:
  (no args)      start the MCP server over stdio (default)
  serve --http   start the server over Streamable HTTP (remote connector)
  init           run the interactive setup wizard
  --version      print the package version
  --help         print this message

HTTP can also be selected with BIBLIOCOMMONS_MCP_TRANSPORT=http. In HTTP
mode, credentials are optional: without them the server runs read-only
"catalog mode". See docs/projects/remote-mcp-mobile.md.
```
